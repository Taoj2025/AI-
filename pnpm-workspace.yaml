# pnpm Workspace 配置
packages:
  # 应用程序
  - "apps/*"
  # 共享包
  - "packages/*"
  # Python 微服务（使用 pip，npm 服务排除）
  - "services/ai-dispatch"
  - "services/export-service"
  - "services/template-service"
  - "services/analytics-service"
  - "services/payment-service"
  # 基础设施（不参与 workspace）
  # infra/ 单独管理
